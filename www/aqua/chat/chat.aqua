import "@fluencelabs/aqua-lib/builtin.aqua"

service Chat("Chat"):
  say(from: PeerId) -> string

func say(targetPeerId: PeerId, targetRelayPeerId: PeerId) -> string:
  on targetPeerId via targetRelayPeerId:
    res <- Chat.say(%init_peer_id%)
  <- res